package org.mpd.app.global;

public class GlobalUrl {

    public static final String ROOT_URI = "/";
    public static final String MAIN_URI = ROOT_URI + "main";

    public static final String LOGIN_URI = ROOT_URI + "login";
    public static final String LOGIN_ACTION = LOGIN_URI + "/action";

    public static final String JOIN_URI = ROOT_URI + "join";
    public static final String JOIN_ACTION = JOIN_URI + "/action";

    public static final String LOGOUT = ROOT_URI + "logout";

}
